[metadata]
name = decomposer
version = 0.1.0

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.9
install_requires =
    numpy>=1.24
    opencv-python>=4.8

[options.packages.find]
where = src

[options.entry_points]
console_scripts =
    decomposer = opal_c1.cli:main
